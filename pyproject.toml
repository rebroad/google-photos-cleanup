[project]
name = "google-photos-cleanup"
version = "0.1.0"
description = "Report-only Android media inventory and Google Photos matching"
requires-python = ">=3.10"

[project.scripts]
gphotos-cleanup = "gphotos_cleanup.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
